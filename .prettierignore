# Schema exporter fixtures are compared byte-for-byte against generator output
# in src/services/schema-export/tests. Formatting them breaks those tests.
# (The .ts fixtures use a .unformatted suffix for the same reason.)
src/services/schema-export/tests/models/

# Not a parseable source file
electron-builder.env.example
